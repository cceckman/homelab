- name: "Configure static IPs"
  become: true
  ansible.builtin.copy:
    dest: /etc/systemd/network/
    src: network/
- name: "Enable, restart networkd"
  become: true
  ansible.builtin.systemd:
    name: systemd-networkd
    enabled: true
    state: restarted

